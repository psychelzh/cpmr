# Collect observation-level predictions from resampling results

Generic for extracting observation-level predictions from resampling
result objects.

## Usage

``` r
collect_predictions(x, ...)
```

## Arguments

- x:

  A resampling result object.

- ...:

  Additional arguments passed to method implementations.

## Value

A data frame or tibble with one row per observation.
