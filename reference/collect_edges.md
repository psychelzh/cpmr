# Collect selected edges from fitted objects

Generic for extracting stored selected-edge outputs.

## Usage

``` r
collect_edges(x, ...)
```

## Arguments

- x:

  A fitted object.

- ...:

  Additional arguments passed to method implementations.

## Value

A matrix, array, or `NULL` depending on how edges were stored.
