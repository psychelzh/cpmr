% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-spec.R
\name{cpm_construction_strength}
\alias{cpm_construction_strength}
\title{Define CPM strength-construction settings}
\usage{
cpm_construction_strength(
  sign_mode = c("separate", "net"),
  weight_scale = 0,
  standardize_edges = FALSE
)
}
\arguments{
\item{sign_mode}{How positive and negative screened information is
represented. \code{"separate"} constructs \code{positive_summary} and
\code{negative_summary}, fits a \code{joint} stream from both together, and also
returns \code{positive} and \code{negative} positive-only / negative-only diagnostic
streams. \code{"net"} constructs one
\code{net_summary = positive_summary - negative_summary} feature and returns a
single \code{net} stream.}

\item{weight_scale}{Non-negative scale controlling optional sigmoid-style
edge weighting before strength construction. \code{0} disables additional
weighting and keeps the classic hard-threshold CPM score. Values greater
than \code{0} apply the current sigmoid weighting scheme, with larger values
producing a smoother transition around the selection cutoff.}

\item{standardize_edges}{Logical value indicating if edge values should be
standardized within each training set before CPM feature construction. If
\code{TRUE}, each edge is centered and scaled to unit variance using the
training data, and the same transformation is applied again at prediction
time. This follows the fold-local edge z-scoring approach described by
Rapuano et al. (2020). Defaults to \code{FALSE} so the default construction
stays close to the classic CPM strength workflow; set it to \code{TRUE}
when you want this additional preprocessing step explicitly.}
}
\description{
Build the feature-construction portion of a \code{\link[=spec]{spec()}}. This helper
configures the current CPM path that turns screened edges into subject-level
strength-style predictors. The name uses "strength" because this stage
follows the CPM tradition of constructing subject-level scores from screened
edge sets, but it is not limited to a simple raw network-strength sum:
positive and negative edges can be represented separately or collapsed into a
net score, optional weighting can be applied before the score is formed, and
edge values can be standardized within each training set before constructing
the final predictors.
}
\examples{
cpm_construction_strength()
cpm_construction_strength(
  sign_mode = "net",
  weight_scale = 0.03
)
}
