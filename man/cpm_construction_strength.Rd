% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-helpers.R
\name{cpm_construction_strength}
\alias{cpm_construction_strength}
\title{Define CPM network-strength construction settings}
\usage{
cpm_construction_strength(
  polarity = c("separate", "net"),
  weighting = cpm_weighting(),
  standardize_edges = FALSE
)
}
\arguments{
\item{polarity}{How positive and negative screened information is represented.
\code{"separate"} constructs \code{positive_strength} and \code{negative_strength}, fits a
\code{joint} stream from both together, and also returns \code{positive} and
\code{negative} single-strength diagnostic streams. \code{"net"} constructs one
\code{net_strength = positive_strength - negative_strength} feature and returns
a single \code{net} stream.}

\item{weighting}{Edge-weighting helper created by \code{\link[=cpm_weighting]{cpm_weighting()}}.}

\item{standardize_edges}{Logical value indicating if edge strengths should be
standardized within each training set before CPM feature construction. If
\code{TRUE}, each edge is centered and scaled to unit variance using the
training data, and the same transformation is applied again at prediction
time. This follows the fold-local edge z-scoring approach described by
Rapuano et al. (2020). Defaults to \code{FALSE} so the default construction
stays close to the classic CPM network-strength workflow; set it to \code{TRUE}
when you want this additional preprocessing step explicitly.}
}
\description{
Build the feature-construction portion of a \code{cpm_spec()}. This helper
configures the current network-strength construction path by deciding how
positive and negative screened edge sets are represented, how screened edges
are weighted, and whether edge standardization is applied before constructing
subject-level predictors.
}
\examples{
cpm_construction_strength()
cpm_construction_strength(
  polarity = "net",
  weighting = cpm_weighting("sigmoid", scale = 0.03)
)
}
