% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-spec.R
\name{cpm_selection_cor}
\alias{cpm_selection_cor}
\title{Define CPM correlation-based edge-selection settings}
\usage{
cpm_selection_cor(
  method = c("pearson", "spearman"),
  criterion = c("p_value", "absolute", "proportion"),
  level = 0.01
)
}
\arguments{
\item{method}{Correlation method used during edge selection. \code{"pearson"} is
the default CPM path; \code{"spearman"} can be useful when a monotonic but
non-linear relationship is expected.}

\item{criterion}{How \code{level} is interpreted. \code{"p_value"} thresholds the
absolute correlation by the critical value implied by \code{level}.
\code{"absolute"} treats \code{level} as a direct absolute-correlation cutoff.
\code{"proportion"} retains a per-sign proportion of positive and negative edges
separately. Because edge counts are discrete and ties at the cutoff are
retained, the realized proportion may be slightly larger than the requested
\code{level}.}

\item{level}{Numeric level associated with \code{criterion}.}
}
\description{
Build the selection portion of a \code{\link[=spec]{spec()}}. This helper configures the
current correlation-based edge-selection path by choosing the correlation
method used to relate each edge to the outcome and how the associated \code{level}
is interpreted.
}
\examples{
cpm_selection_cor()
cpm_selection_cor(method = "spearman", criterion = "absolute", level = 0.1)
}
