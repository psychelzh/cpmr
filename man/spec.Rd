% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-spec.R
\name{spec}
\alias{spec}
\alias{cpm}
\title{Define a CPM specification}
\usage{
spec(
  selection = cpm_selection_cor(),
  construction = cpm_construction_summary(),
  model = cpm_model_lm()
)

cpm(
  conmat,
  behav,
  spec = NULL,
  ...,
  covariates = NULL,
  resamples = NULL,
  return_edges = c("sum", "none", "all"),
  na_action = c("fail", "exclude")
)
}
\arguments{
\item{selection}{Selection helper created by \code{\link[=cpm_selection_cor]{cpm_selection_cor()}}.}

\item{construction}{Construction helper created by
\code{\link[=cpm_construction_summary]{cpm_construction_summary()}}.}

\item{model}{Outcome-model helper created by \code{\link[=cpm_model_lm]{cpm_model_lm()}}. This stage
maps CPM-derived subject-level features to the behavioral outcome.}

\item{conmat}{A matrix of connectome data. Observations in row, edges in
column.}

\item{behav}{A numeric outcome vector with one value per observation in
\code{conmat}. Row or column matrices are accepted and converted with \code{\link[=drop]{drop()}}.}

\item{spec}{A CPM specification created by \code{\link[=spec]{spec()}}. If \code{NULL}, \code{\link[=spec]{spec()}} is
used.}

\item{...}{For future extension. Currently ignored.}

\item{covariates}{A matrix of covariates. Observations in row, variables in
column. If \code{NULL}, no covariates are used. Vectors are converted to
single-column matrices.}

\item{resamples}{Optional resampling specification. Use \code{NULL} for leave-one-
out resampling, a single integer greater than or equal to 2 to request
k-fold resampling, or a list of assessment indices for manual folds. Manual
resamples must contain integer vectors indexing rows in \code{conmat}.}

\item{return_edges}{A character string indicating the return value of the
selected edges. \code{"sum"} stores fold counts for each selected edge,
\code{"none"} skips edge storage, and \code{"all"} stores the fold-wise edge masks.}

\item{na_action}{A character string indicating the action when missing values
are found in the inputs. \code{"fail"} stops immediately when any required value
is missing. \code{"exclude"} fits on complete cases and keeps the original row
layout in the returned predictions.}
}
\value{
\code{spec()} returns a CPM specification object that can be reused across CPM
runs.

\code{cpm()} returns a \code{cpm} object containing observation-level predictions,
resampling folds, and optional stored edges. Call \code{\link[=summary.cpm]{summary.cpm()}} for the
default aggregate report, or \code{\link[=resample_metrics]{resample_metrics()}} when you want pooled or
fold-wise metrics in tabular form.
}
\description{
Create a lightweight specification object that stores the staged decisions
required to run connectome-based predictive modeling later with \code{\link[=cpm]{cpm()}}.

\code{cpm()} is the primary CPM workflow. It applies a CPM specification under a
resampling plan and returns a \code{cpm} object with out-of-fold predictions.
}
\details{
\code{spec()} keeps the main CPM decisions visible at the top level while
grouping stage-specific settings into helpers.

\code{spec()} groups CPM settings into three stages:
\itemize{
\item \code{\link[=cpm_selection_cor]{cpm_selection_cor()}} configures the current correlation-based edge
selection path.
\item \code{\link[=cpm_construction_summary]{cpm_construction_summary()}} configures the current summary-construction
path.
\item \code{\link[=cpm_model_lm]{cpm_model_lm()}} defines the outcome model fitted on CPM-derived features.
}
}
\examples{
s <- spec(
  selection = cpm_selection_cor(
    method = "spearman",
    criterion = "absolute",
    level = 0.1
  ),
  construction = cpm_construction_summary(
    sign_mode = "net",
    weight_scale = 0.03
  ),
  model = cpm_model_lm()
)
s

conmat <- matrix(rnorm(100 * 100), nrow = 100)
behav <- rnorm(100)
resample_obj <- cpm(
  conmat = conmat,
  behav = behav,
  spec = s,
  resamples = 5
)
summary(resample_obj)
}
