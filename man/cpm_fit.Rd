% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-native-api.R
\name{cpm_fit}
\alias{cpm_fit}
\alias{cpm_fit_resamples}
\title{Direct native CPM fit helpers}
\usage{
cpm_fit(
  conmat,
  behav,
  covariates = NULL,
  thresh_method = c("alpha", "sparsity"),
  thresh_level = 0.01,
  bias_correct = TRUE,
  na_action = c("fail", "exclude")
)

cpm_fit_resamples(
  conmat,
  behav,
  covariates = NULL,
  resamples = NULL,
  kfolds = NULL,
  thresh_method = c("alpha", "sparsity"),
  thresh_level = 0.01,
  bias_correct = TRUE,
  return_edges = c("sum", "none", "all"),
  na_action = c("fail", "exclude")
)
}
\arguments{
\item{conmat}{A matrix of connectome data. Observations in rows and edges in
columns.}

\item{behav}{A numeric vector of behavioral data. Length must equal the
number of rows in \code{conmat}.}

\item{covariates}{Optional covariate matrix. Observations in rows and
covariates in columns.}

\item{thresh_method, thresh_level, bias_correct}{CPM modeling parameters passed
through to \code{\link[=cpm_spec]{cpm_spec()}}.}

\item{na_action}{How to handle missing values. \code{"fail"} errors when missing
values are present; \code{"exclude"} fits on complete cases and preserves row
positions in outputs.}

\item{resamples}{Optional list of assessment indices for custom resampling.}

\item{kfolds}{Number of folds used when \code{resamples} is \code{NULL}.}

\item{return_edges}{How selected edges should be stored for
\code{cpm_fit_resamples()}. \code{"sum"} stores fold-summed edges, \code{"all"} stores
fold-wise edge arrays, and \code{"none"} skips edge storage. \code{cpm_fit()}
always stores the single-fit edge mask by default.}
}
\value{
\code{cpm_fit()} returns a fitted \code{cpm} object.

\code{cpm_fit_resamples()} returns a \code{cpm_resamples} object.
}
\description{
\code{cpm_fit()} and \code{cpm_fit_resamples()} provide direct native entry points for
the most common CPM workflows without requiring users to construct a
\code{\link[=cpm_spec]{cpm_spec()}} object explicitly first.
}
\details{
These helpers currently wrap the same CPM specification and core fitting
paths used by \code{\link[=fit]{fit()}} and \code{\link[=fit_resamples]{fit_resamples()}}, but make the package's native
\code{conmat}/\code{behav} workflow more discoverable.
}
\examples{
conmat <- matrix(rnorm(100), ncol = 10)
behav <- rnorm(10)

fit_obj <- cpm_fit(conmat, behav)
res_obj <- cpm_fit_resamples(conmat, behav, kfolds = 5)
}
