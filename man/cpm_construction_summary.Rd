% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpm-spec-helpers.R
\name{cpm_construction_summary}
\alias{cpm_construction_summary}
\title{Define CPM summary-construction settings}
\usage{
cpm_construction_summary(
  polarity = c("separate", "net"),
  weight_scale = 0,
  standardize_edges = FALSE
)
}
\arguments{
\item{polarity}{How positive and negative screened information is represented.
\code{"separate"} constructs \code{positive_summary} and \code{negative_summary}, fits a
\code{joint} stream from both together, and also returns \code{positive} and
\code{negative} positive-only / negative-only diagnostic streams. \code{"net"}
constructs one \code{net_summary = positive_summary - negative_summary}
feature and returns a single \code{net} stream.}

\item{weight_scale}{Non-negative scale controlling optional sigmoid-style
edge weighting before summary construction. \code{0} disables additional
weighting and keeps the classic hard-threshold CPM summary. Values greater
than \code{0} apply the current sigmoid weighting scheme, with larger values
producing a smoother transition around the selection cutoff.}

\item{standardize_edges}{Logical value indicating if edge values should be
standardized within each training set before CPM feature construction. If
\code{TRUE}, each edge is centered and scaled to unit variance using the
training data, and the same transformation is applied again at prediction
time. This follows the fold-local edge z-scoring approach described by
Rapuano et al. (2020). Defaults to \code{FALSE} so the default construction
stays close to the classic CPM summary workflow; set it to \code{TRUE}
when you want this additional preprocessing step explicitly.}
}
\description{
Build the feature-construction portion of a \code{cpm_spec()}. This helper
configures the current summary-construction path by deciding how positive and
negative screened edge sets are represented, whether screened edges are
additionally weighted before summary construction, and whether edge
standardization is applied before constructing subject-level predictors.
}
\examples{
cpm_construction_summary()
cpm_construction_summary(
  polarity = "net",
  weight_scale = 0.03
)
}
