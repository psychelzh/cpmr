---
title: "Choosing a Workflow in cpmr"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Choosing a Workflow in cpmr}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

## Overview

`cpmr` supports more than one way to express a CPM analysis, but they do not
all serve the same purpose.

The package is organized around a core-first design:

- the native `cpmr` path is the primary workflow for fitting, resampling, and
  future tuning extensions;
- specification-driven helpers such as `cpm_spec()` remain useful when you want
  explicit parameter objects;
- any future tidymodels adapter should be treated as optional interoperability,
  not as the main package identity.

## Recommended Path: Native Helpers

For most analyses, start with `cpm_fit()` and `cpm_fit_resamples()`.

```{r}
library(cpmr)

set.seed(123)
conmat <- matrix(rnorm(80 * 200), nrow = 80)
behav <- rnorm(80)

fit_obj <- cpm_fit(conmat, behav)
resample_obj <- cpm_fit_resamples(conmat, behav, kfolds = 5)

summary(fit_obj)
collect_metrics(resample_obj)
```

This path is the best fit when you care about:

- large edge spaces;
- repeated resampling;
- leakage-safe covariate handling;
- future CPM-specific performance improvements such as fold-level reuse.

## Lower-Level Native Path: `cpm_spec()`

If you want an explicit specification object, you can still work with
`cpm_spec()` plus `fit()` and `fit_resamples()`.

```{r}
spec <- cpm_spec(thresh_method = "sparsity", thresh_level = 0.2)

fit_via_spec <- fit(spec, conmat = conmat, behav = behav)
resamples_via_spec <- fit_resamples(spec, conmat = conmat, behav = behav, kfolds = 5)
```

This route remains native `cpmr`; it is just a slightly lower-level interface.

## Where an External Workflow Adapter Fits

`cpmr` does not currently ship a dedicated external workflow adapter surface.
If future interoperability work is revisited, it should remain:

- optional rather than required;
- thin relative to the native CPM engine;
- useful for interoperability, not positioned as the default execution path.

It should not redefine the package around generic workflow orchestration. For
high-dimensional resampling and tuning-heavy workloads, the native path is
expected to remain the recommended route.

## Practical Rule of Thumb

Choose the native path when you need speed, explicit CPM behavior, or
resampling-heavy experiments.

Choose an external workflow adapter only when interoperability is worth the
extra abstraction and potential overhead.
