## Resubmission

This is a patch release (0.1.1).

## R CMD check results

Local check command:

`R CMD check --as-cran --no-manual cpmr_0.1.1.tar.gz`

Result: 0 errors | 0 warnings | 2 notes.

Notes:

1. `unable to verify current time`
2. `Files 'README.md' or 'NEWS.md' cannot be checked without 'pandoc' being installed.`

Both notes are due to the local environment and are expected.
